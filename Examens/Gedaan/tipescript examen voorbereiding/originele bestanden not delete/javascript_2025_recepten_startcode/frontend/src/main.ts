import 'bootstrap'
import 'bootstrap/dist/css/bootstrap.css'

/**
 * TODO: Registreer hier je custom elements en zet de router op.
 *
 * - Registreer de navbar (verplicht als 'custom-navbar') en je andere custom elements.
 * - Maak een Router aan met de routes '/' (recepten) en '/menu' (weekmenu).
 *
 * Zie de opgave: "Routing & componenten".
 **/
