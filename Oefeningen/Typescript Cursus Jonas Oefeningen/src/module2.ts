// Oefening 1const getallen: number[] = [4, 5, 4, 3, 3];const som: number = getallen.reduce((teller, getal) => teller + getal, 0);console.log(som);// Oefening 2const namen: string[] = ["Jeroen", "Manon", "Annabelle"];// Mijn oplossingfor (const name of namen) {    let namen: string[] = [];    namen.push(name.toUpperCase());    console.log(namen.toString());}// Efficiëntere maniernamen.forEach(naam => console.log(naam.toUpperCase()));// Oefening 3let taken = []; // Het is onveilig omdat de array als type any word gezien, waardoor je nu zoals in javascript verschillende datatypes erin kan steken.const taken2: string[] = ["Jeroen", "Manon"] // Door :string[] toe te voegen, vertel je dat hier enkel waarden in mogen van het datatype string.// Oefening 4const product: [string, number] = ["OnePlus 12R", 799]console.log(product);console.log(product.toString());console.log(`${product[0]} voor ${product[1]} euro`);// Oefening 5enum Richting {    Noord,    Oost,    Zuid,    West}const richting: Richting = Richting.Noord;if (richting === Richting.Noord) {    console.log(`Je gaat in de juiste richting en dat is ${Richting[richting]}`)}console.log();// Oefening 6enum Richting2 {    Noord = "NOORD",    Oost = "OOST",    Zuid = "ZUID",    West = "WEST"};const richting2: Richting2 = Richting2.Noord;if (richting2 === Richting2.Noord) {    console.log(`Je gaat in de juiste richting en dat is ${richting2}`)}console.log();